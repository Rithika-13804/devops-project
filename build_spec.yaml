version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

steps:
  - type: Command
    name: "Build Step"
    command: |
      echo "OCI DevOps Build Started"
      ls -la
      mvn clean install

outputArtifacts:
  - name: build_output
    type: BINARY
    location: target
