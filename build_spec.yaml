version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

phases:
  build:
    commands:
      - echo "OCI DevOps build started"
      - ls -la
      - mvn clean install

artifacts:
  files:
    - '**/*'
